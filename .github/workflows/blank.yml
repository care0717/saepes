name: CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run install
      run: yarn
    - name: Run test
      run: yarn test --watchAll=false
